<!DOCTYPE html>
<html lang="pt-BR">
 <head>
  <script>
   (function () {
  var proto = XMLHttpRequest.prototype;
  var open = proto.open, setRequestHeader = proto.setRequestHeader, send = proto.send;
  var TOKEN_KEY = 'base44_access_token';
  var storedTokenHeader = function () {
    try { return 'Bearer ' + localStorage.getItem(TOKEN_KEY); } catch (e) { return null; }
  };
  var forgetStoredToken = function () {
    try { localStorage.removeItem(TOKEN_KEY); localStorage.removeItem('token'); } catch (e) {}
  };

  proto.open = function (method, url) {
    this.__b44IsAuthCheck = /\/entities\/User\/me(\?|$)/.test(url);
    return open.apply(this, arguments);
  };

  proto.setRequestHeader = function (name, value) {
    if (this.__b44IsAuthCheck && name.toLowerCase() === 'authorization' && value === storedTokenHeader()) {
      this.__b44SentToken = value;
    }
    return setRequestHeader.call(this, name, value);
  };

  proto.send = function () {
    this.addEventListener('loadend', function () {
      var rejectedTokenStillStored = this.status === 401 && this.__b44SentToken === storedTokenHeader();
      if (rejectedTokenStillStored) forgetStoredToken();
    });
    return send.apply(this, arguments);
  };
})();
  </script>
  <meta charset="utf-8"/>
  <link href="/favicon.svg" rel="icon" type="image/svg+xml"/>
  <meta content="width=device-width, initial-scale=1.0, viewport-fit=cover" name="viewport"/>
  <meta content="#1E2D4E" name="theme-color"/>
  <meta content="yes" name="apple-mobile-web-app-capable"/>
  <meta content="black-translucent" name="apple-mobile-web-app-status-bar-style"/>
  <meta content="Capibaribe" name="apple-mobile-web-app-title"/>
  <meta content="Capibaribe Consultoria — estruturação financeira, captação de recursos (BNDES, BNB, BASA, FINEP) e inteligência tributária (SUDENE, PRODEPE). A ponte entre o capital e o desenvolvimento." name="description"/>
  <meta content="captação de recursos, BNDES, BNB, SUDENE, PRODEPE, inteligência tributária, project finance, FIDC, debêntures, valuation, M&amp;A, emendas parlamentares, PPA, LDO, LOA, consultoria financeira Pernambuco" name="keywords"/>
  <meta content="Capibaribe Consultoria" property="og:title"/>
  <meta content="Estruturação financeira e inteligência tributária. A ponte entre o capital e o desenvolvimento do Brasil." property="og:description"/>
  <meta content="website" property="og:type"/>
  <meta content="pt_BR" property="og:locale"/>
  <link href="https://fonts.googleapis.com" rel="preconnect"/>
  <link crossorigin="" href="https://fonts.gstatic.com" rel="preconnect"/>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800;900&amp;family=JetBrains+Mono:wght@400;500;600&amp;display=swap" rel="stylesheet"/>
  <title>
   Capibaribe Consultoria | Estruturação Financeira e Inteligência Tributária
  </title>
  <script crossorigin="" src="/assets/index-DTS-K7f_.js" type="module">
  </script>
  <link crossorigin="" href="/assets/index-B_8FUjV5.css" rel="stylesheet"/>
  <script type="module">
   if (window.self === window.top) {
  let lastPath = "";
  function getPageNameFromPath(path) {
    const segments = path.split("/").filter(Boolean);
    return segments[0] || null;
  }
  function trackPageView() {
    const path = window.location.pathname;
    if (path === lastPath) return;
    lastPath = path;
    const pageName = getPageNameFromPath(path) || "home";
    const appId = "6a4ed5b422804a8244ed8da5";
    if (!appId) return;
    fetch(`/api/app-logs/${appId}/log-user-in-app/${pageName}`, {
      method: "POST",
    }).catch(() => {});
  }
  const originalPushState = history.pushState.bind(history);
  history.pushState = function (...args) {
    originalPushState(...args);
    trackPageView();
  };
  const originalReplaceState = history.replaceState.bind(history);
  history.replaceState = function (...args) {
    originalReplaceState(...args);
    trackPageView();
  };
  window.addEventListener("popstate", trackPageView);
  trackPageView();
}
  </script>
  <meta content="yes" name="mobile-web-app-capable"/>
  <script async="true" data-app-id="6a4ed5b422804a8244ed8da5" data-platform-url="https://app.base44.com" src="/static/js/badge.js">
  </script>
 </head>
 <body>
  <div id="root">
  </div>
 </body>
</html>
